'use strict';

// Represents a successful operation
const STATUS_OK = 0;

// Database related error
const ERR_DB = -1;

// HTTP fetch related error
const ERR_FETCH = -2;

// Parsing error
const ERR_PARSE = -3;

// DOM related error
const ERR_DOM = -4;
