

document.addEventListener('DOMContentLoaded', function() {
  'use strict';
  // TODO: setup the test. create a basic document
  // attach some nodes to it.
  // For unwrap, test basic unwrap.
  // Test unwrap orphan fails.
  // Test left only padding needed.
  // Test right only padding needed.
  // Test both padding needed.
  // Test no-children unwrap
  // Test invalid hierarchy unwrap (e.g put a <tr> instead a <h1> or something)

  // TODO: setup a second document or reset the document state so that the
  // tests are independent.
  // For rename, test basic rename
  // Test rename of null/undefined failure
  // Test rename of empty string failure
  // Test rename to exotic name not in html5 set
  // Test non-copy attributes
  // Test no children rename


}, {'once': true});
